# ── Variables de entorno (NUNCA subir al repo) ────────────────────────────────
.env

# ── Índice FAISS: NO excluir — debe estar en el repo para Streamlit Cloud ─────
# db/   ← comentado intencionalmente: el índice es estático y debe versionarse
db/.gitkeep

# ── Caché de modelos HuggingFace ─────────────────────────────────────────────
.cache/
__pycache__/
*.pyc
*.pyo

# ── IDEs ──────────────────────────────────────────────────────────────────────
.vscode/
.idea/

# ── Python environments ───────────────────────────────────────────────────────
venv/
.venv/
env/

# ── Streamlit secrets (NUNCA subir — configurar en el dashboard de Cloud) ─────
.streamlit/secrets.toml
